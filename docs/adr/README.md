# Architecture Decision Records

One file per decision: `NNNN-title.md`, format = Context / Decision / Consequences.

Only write one when a real fork in the road happens (e.g. "why modular monolith,
not microservices" or "why extract a service"). Don't pre-create ADRs for
decisions Phase 0 already settled in `CLAUDE.md`.
